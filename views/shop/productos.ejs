<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/product.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>

    <main>
        <% if (prods.length > 0) { %>
            <div class="grid">
                <% for (let producto of prods) { %>
                    <article class="card product-item">
                        <header class="card__header">
                            <h1 class="product__title"><%= producto.nombre %></h1>
                        </header>
                        <div class="card__image">
                            <img src="<%= producto.imagenUrl %>" alt="<%= producto.nombre %>">
                        </div>
                        <div class="card__content">
                            <h2 class="product__price"><%= producto.precio %></h2>
                            <h2 class="product__cantidad"><%= producto.cantidad %></h2>
                            <p class="product__descripcion"><%= producto.descripcion %></p>
                        </div>
                        <div class="card__actions">
                            <a href="/productos/<%= producto.id %>" class="btn">Detalle</a>
                            <form action="/agregarCarrito" method="POST">
                                <button class="btn">Agregar al Carrito</button>
                            </form>    
                        </div>
                    </article>
                <% } %>
            </div>
        <% } else { %>
            <h1>No Products Found!</h1>
        <% } %>
    </main>
<%- include('../includes/end.ejs') %>